{
  "name": "veyla-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "landing": "cd packages/landing && npm run dev",
    "dashboard": "cd packages/dashboard && npm run dev",
    "extension:build": "cd packages/extension && npm run build",
    "extension:package": "cd packages/extension && npm run package",
    "build": "npm run build --workspaces --if-present"
  },
  "resolutions": {
    "@types/react": "^19.0.0",
    "next": "^14.0.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
